packages:
  - package: dbt-labs/dbt_utils
    version: 1.1.1

# Check for any exsisting packages on https://hub.getdbt.com/
# To add packages to your project create a packages.yml file 
# execute dbt deps (dependancy)
